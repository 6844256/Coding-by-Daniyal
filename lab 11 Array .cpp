//__________________________lab 11 Array ______________________
#include <iostream>
using namespace std;

int main() {
//___________________________program 01 _____________________________
    /* int array[5]={6,7,2,9,4};
     cout << "Array elements are:" << endl;
     for(int i=0;i<5;i++){
    cout <<array[i]<<endl;
    }
  return 0;
  }*/
  
  //______________________program 02_________________
   /* int N;
    cout << "Enter the number of elements (N): ";
    cin >> N;

    int arr[N];
    cout << "Enter " << N << " integers: ";
    for (int i = 0; i < N; i++) {
    
        cin >> arr[i];
    }

    cout << "Elements in reversed order: ";
    for (int i = N - 1; i >= 0; i--) {
        cout << arr[i] << endl;
    }

    return 0;
}
*/
//_________________________program 03________________________
/*int arr[]={4,4,6};
int n=sizeof(arr)/sizeof(arr[0]);
int sum=0;
for(int i=0;i<n;i++){
sum+=arr[i];
}
double average =(double)sum/n;
cout << "sum of arry is="<<sum << endl;
cout << "average of arry is=" <<average<< endl;
return 0;
}
*/
//_________________________program 04________________________
/*int arr[]={57,5,6,7, 8, 8,2,8};
double length_of_array=sizeof(arr)/sizeof (arr[0]);
cout << "length of array is="<<length_of_array ;
return 0;
}
*/
//_________________________program 05_________________________
  
  /*  int N;
    cout << "Enter the number of elements (N): ";
    cin >> N;

    int arr[N];
    cout << "Enter " << N << " integers: ";
    for (int i = 0; i < N; i++) {
    cin >> arr[i];
    }

    int max = arr[0] ;
     for (int i = 1; i < N; i++) {
        if (arr[i] > max) 
            max = arr[i];
        
    }

    cout << "Largest number in array: " << max << endl;

    return 0;
}*/
    